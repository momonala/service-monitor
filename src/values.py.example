# Stub values for CI/testing - copy to values.py for local development

telegram_api_token = "0000000000:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
telegram_chat_id = "0000000000"

# GitHub token for accessing Actions API (avoids rate limits, enables private repo access)
# Create at: https://github.com/settings/tokens
# Required scope: public_repo (for public repos) or repo (for private repos)
GITHUB_TOKEN = "ghp_dummy_token_for_testing"

INSPECTOR_DETECTOR_UV_PATH = "/usr/bin/uv"
INSPECTOR_DETECTOR_CWD = "/tmp/inspector_detector"
